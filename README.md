# ChatterBot

A Raspberry Pi "companion bot" head — a Python project.

## Overview

ChatterBot is a companion robot head built on a Raspberry Pi.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
